/**
 * @tahanabavi/type-devtools
 * =========================
 * React inspector panel + transport adapters. Renders any bridge from
 * @tahanabavi/type-devtools-core and tags each row by source.
 *
 * Scaffold — the panel and the `connectTypeFetch` / (later) `connectTypeSocket`
 * adapters live here. See ../../docs/ARCHITECTURE.md.
 */

export type { InspectorEvent, InspectorSource } from "@tahanabavi/type-devtools-core";

export const version = "0.0.0";
