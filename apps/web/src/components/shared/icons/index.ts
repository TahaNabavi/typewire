/**
 * Official artwork from simple-icons (CC0), inlined rather than installed: the
 * site needs four glyphs, not three thousand, and inlining keeps them in the
 * server-rendered HTML with no request and no client bundle.
 *
 * They are drawn in `currentColor` on purpose. These are used at 14px beside
 * mono text, where full-colour brand marks would read as a row of stickers
 * pasted onto a technical line; monochrome makes them behave as typography and
 * lets them pick up the theme.
 */
export { default as BunIcon } from './BunIcon'
export { default as DenoIcon } from './DenoIcon'
export { default as NodeIcon } from './NodeIcon'
export { default as NpmIcon } from './NpmIcon'
